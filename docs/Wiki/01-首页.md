# Distortion Knight Wiki 首页

> Unity 卡牌 Roguelike 游戏 — 技术开发文档

## 架构简述

Distortion Knight 采用 **GameManager 门面模式** 统一调度四大核心子系统：地图探索、卡牌战斗、玩家数据、UI 管理。各子系统内部由对应的单例管理器（BattleManager、HandManager、MapManager 等）独立运作，通过 C# 事件/委托进行跨系统通信。

核心数据使用 **ScriptableObject** 驱动（牌组、奖励池、地图配置、节点蓝图、战斗背景），卡牌效果采用 **策略模式**（CardEffect 抽象基类 + 19 个具体子类），敌人通过 **工厂方法** 创建并配备独立的 AI 行为模式。

## 开发环境搭建

### 前置条件

1. 安装 [Unity Hub](https://unity.com/download)
2. 安装 **Unity 2022.3.62f3c1**（通过 Unity Hub → Installs → Add → 2022.3.62f3c1）
3. 安装模块：Universal Render Pipeline、Windows Build Support、TextMesh Pro（通常随 Unity 自带）

### 克隆仓库

```bash
git clone https://gitee.com/mengge237/distortion-knight-development.git
```

### 打开项目

1. 启动 Unity Hub
2. 点击 **Add project from disk**
3. 选择仓库根目录（包含 `Assets/`、`Library/` 等文件夹的目录）
4. 等待 Unity 完成首次导入（Library 已包含则速度较快）
5. 打开场景：`Assets/_Project/Scenes/MainScene.unity`

### 第三方依赖

| 插件 | 路径 | 说明 |
|------|------|------|
| DOTween | `Assets/Plugins/Demigiant/DOTween/` | 动画补间库，已包含在仓库中 |
| TextMesh Pro | `Assets/TextMesh Pro/` | UI 文本渲染，Unity 内置插件 |

无需额外安装包管理器或导入外部资源。

### AI/MCP 集成（Claude Code 直接操作 Unity）

项目已内置 [MCP for Unity](https://github.com/CoplayDev/unity-mcp)（`Packages/manifest.json` 中 `com.coplaydev.unity-mcp`，Unity 2022.3 兼容），Claude Code 通过根目录 `.mcp.json` 连接 `http://localhost:8080/mcp`。

1. 前置依赖：Python 3.11+ 与 [uv](https://docs.astral.sh/uv/)（本机已装 3.13 / uv 0.10）
2. Unity 首次导入后会自动弹出设置向导，确认 Python/uv → Done
3. 日常使用：`Window → MCP for Unity → Start Server`，状态面板显示 Connected
4. 重启 Claude Code 会话（或 `/mcp` 重连），即可获得 Unity 操作工具（改场景/资产、执行菜单命令、运行 C# 等）
5. Unity 未启动时 MCP 工具不可用，Claude 会退回直接修改文件的方式

## 文档索引

| 文档 | 说明 |
|------|------|
| [架构设计](./02-架构设计.md) | ASCII 架构图、核心系统详解、设计模式汇总、事件系统表 |
| [代码约定](./03-代码约定.md) | 单例规范、命名规范、注释规范、提交信息格式 |
| [已知局限](./04-已知局限.md) | 空实现列表、硬编码问题、资源错放（已解决项归档） |
| [项目概览](../知识库/01-项目概览.md) | 玩法介绍、技术栈、项目目录树 |
| [开发进度](../知识库/02-开发进度.md) | 各系统实现状态、待办事项 |
| [开发日志](../知识库/04-开发日志.md) | 按日期整理的开发记录 |

## Wiki 同步说明

gitee 的 Wiki 仓库拒绝一切 git 推送（服务端 hook 拦截，含空提交），只能通过 gitee 网页端「Wiki」页编辑。本目录与 `docs/知识库/` 是文档的权威副本，如需要更新 gitee Wiki，请打开 https://gitee.com/mengge237/distortion-knight-development/wiki 将对应 `.md` 内容粘贴到网页编辑器。
