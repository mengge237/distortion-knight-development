# 开发进度

> 最后更新：2026-07-25

## 各系统实现状态

| 系统 | 状态 | 说明 |
|------|------|------|
| 地图生成 | 基础完成 | 程序化节点生成、连线、可达性检查、地图贴图显示 |
| 地图交互 | 基础完成 | 节点点击、玩家移动动画、相机跟随 |
| 战斗系统 | 核心完成 | 回合制流程、能量/抽牌/出牌/弃牌/格挡 |
| 卡牌系统 | 核心完成 | 12 张卡牌（攻击/防御/技能/能力）、升级机制 |
| 卡牌效果 | 核心完成 | 19 个效果子类（策略模式），部分空实现待补全 |
| 敌人系统 | 核心完成 | 10 种敌人、AI 行为模式、意图系统、Boss 二阶段 |
| 奖励系统 | 基础完成 | 金币+卡牌奖励面板，普通/精英/Boss 奖池 |
| 玩家数据 | 基础完成 | 生命值/金币/Buff 系统、牌组运行时管理 |
| UI 系统 | 基础完成 | 手牌扇形布局、战斗日志、状态栏、设置面板 |
| 事件系统 | 未开始 | Event 目录为空，无随机事件实现 |
| 商店系统 | 未开始 | 商店节点类型已定义，无购买逻辑 |
| 休息节点 | 未开始 | 休息节点类型已定义，无恢复/升级逻辑 |

## 当前开发中的功能

- 战斗胜利后的流程衔接（`GameManager.OnBattleEnd` 胜利分支为空）
- 玩家 Buff 系统完善（`ApplyTemporaryStrengthEffect`/`ApplyDexterityEffect` 的玩家侧空实现）
- Boss 腐化君王的力量加成逻辑（`Enemy.OnTurnStart` 中 `bonus > 0` 分支为空）
- `ModifyCostEffect` 的 `applyToAllHand` 分支逻辑

## 待办事项（按优先级排列）

### P0 - 必须完成

| 事项 | 关联文件 | 说明 |
|------|----------|------|
| 战斗胜利流程 | `GameManager.cs` | `OnBattleEnd(victory=true)` 需实现：恢复玩家、显示奖励面板、返回地图 |
| `ExhaustCardEffect` 逻辑 | `Effects/ExhaustCardEffect.cs` | 当前 Execute 几乎为空，需在 HandManager 中实现消耗移除 |
| 玩家 Buff 支持 | `ApplyTemporaryStrengthEffect.cs`, `ApplyDexterityEffect.cs` | targetPlayer 分支空实现 |

### P1 - 重要

| 事项 | 关联文件 | 说明 |
|------|----------|------|
| Boss 二阶段加成 | `Enemy.cs` | `GetKingPowerBonus` 返回值 > 0 但 `OnTurnStart` 中未使用 |
| `ModifyCostEffect` 全手牌 | `Effects/ModifyCostEffect.cs` | `applyToAllHand=true` 分支体为空 |
| 事件节点实现 | `Map/` + 新脚本 | 随机事件选择和效果执行 |
| 商店节点实现 | `Map/` + 新脚本 | 金币购买卡牌/遗物/药水 |
| 休息节点实现 | `Map/` + 新脚本 | 恢复生命或升级一张牌 |

### P2 - 优化

| 事项 | 关联文件 | 说明 |
|------|----------|------|
| 敌人数据 SO 化 | `Enemy.cs` | 当前使用硬编码工厂方法，应迁移到 ScriptableObject |
| 节点概率 SO 化 | `MapGenerator.cs` | 节点生成概率硬编码，应提取到 MapConfig |
| 角色专属卡牌池 | `RewardPoolManager.cs` | IroncladCards/SilentCards/DefectCards 为空列表 |
| JSON 数据加载 | `StreamingAssets/CardJson/` | 目录已创建但为空，应实现 JSON → 卡牌配置 |
| 资源目录整理 | `TextMesh Pro/Resources/` | 项目资产（卡图、敌人精灵）错误放在 TMP Resources 下 |

## 代码修复验证清单（2026-07-25）

以下修改已执行，待在 Unity 中打开项目确认编译无报错：

- [ ] Core/CardData.cs — 删除 card.cardId = template.cardId
- [ ] Core/Card.cs — 删除未使用的 upgradeDescription 字段
- [ ] Core/PlayerDataManager.cs — 修复全部中文注释乱码
- [ ] Effects/ModifyCostEffect.cs — 补充 applyToAllHand 分支逻辑
- [ ] Effects/DiscardRandomCardEffect.cs — 删除乱码注释
- [ ] Effects/DealDamageNextTurnEffect.cs — 删除乱码注释
- [ ] Effects/ApplyTemporaryStrengthEffect.cs — 删除复述代码注释
- [ ] Core/CardVisualConfig.cs — 删除未使用的 GetCropRect
- [ ] Core/DeckData.cs — 删除未使用的 GetRandomCards
- [ ] Battle/EnemyAIPattern.cs — 修复中文匹配 + 清理装饰注释
- [ ] Battle/Enemy.cs — 修复条件死循环、删除空块、修复共享对象、删除未用字段
- [ ] Battle/BackgroundConfig.cs — 枚举 地狱复仇骑士 → 腐化君王
- [ ] Battle/BattleManager.cs — 拆分 null 误判
- [ ] Battle/RewardPoolManager.cs — 清理装饰注释
- [ ] Map/Generation/MapGenerator.cs — 负索引检查、GenerateMap 改 public、删除死代码
- [ ] GameManager.cs — 删除空胜利块和死订阅
- [ ] UI/HandManager.cs — try/finally 防死锁、修复同引用添加牌
- [ ] Core/CardUI.cs — OnEndDrag 防销毁后访问
- [ ] UI/RewardPanel.cs — 占位符 ? → G 和 ✓
- [ ] CameraController.cs — cam null 检查、清理装饰注释
- [ ] 文件夹 Map/Gemeration → Map/Generation（已重命名）
